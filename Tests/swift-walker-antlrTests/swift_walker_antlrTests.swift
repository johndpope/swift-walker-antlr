import XCTest
@testable import swift_walker_antlr

class swift_walker_antlrTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        XCTAssertEqual(swift_walker_antlr().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
